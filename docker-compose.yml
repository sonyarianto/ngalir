services:
  webhook:
    image: ngalir/ngalir:latest
    build:
      context: .
      dockerfile: Dockerfile
    entrypoint: ["na-webhook"]
    ports:
      - "8080:8080"
      - "9091:9091"
    environment:
      - NGALIR_NODE_PATH=/usr/local/bin
      - RUST_LOG=info
    volumes:
      - ngalir-state:/var/lib/ngalir
      - ngalir-vault:/etc/ngalir/vault
    restart: unless-stopped

  schedule:
    image: ngalir/ngalir:latest
    entrypoint: ["na-schedule"]
    ports:
      - "9092:9092"
    environment:
      - NGALIR_NODE_PATH=/usr/local/bin
      - RUST_LOG=info
    volumes:
      - ngalir-state:/var/lib/ngalir
      - ngalir-vault:/etc/ngalir/vault
    restart: unless-stopped

volumes:
  ngalir-state:
  ngalir-vault:
