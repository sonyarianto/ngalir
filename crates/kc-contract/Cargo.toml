[package]
name = "kc-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared Node Contract types for Kucur (manifest, exit codes, helpers)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
