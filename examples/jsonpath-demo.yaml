version: 1
name: jsonpath-demo
description: "Extract a field from JSON using na-jsonpath"
nodes:
  - id: src
    use: echo
    with:
      message: hello
  - id: extract
    use: jsonpath
    inputs:
      data: src.echo
    with:
      filter: .
