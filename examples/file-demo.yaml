version: 1
name: file-demo
description: "Write a file, then read it back in the same flow"
nodes:
  - id: writer
    use: file
    with:
      action: write
      path: /tmp/ngalir-demo.txt
      content: "hello from Ngalir"
  - id: reader
    use: file
    with:
      action: read
      path: /tmp/ngalir-demo.txt
